<Window x:Class="EPLAN_API_TUTORIAL.Views.ProjectPropertiesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:hc="https://handyorg.github.io/handycontrol"
        xmlns:local="clr-namespace:EPLAN_API_TUTORIAL.Views"
        xmlns:vm="clr-namespace:EPLAN_API_TUTORIAL.ViewModels"
        mc:Ignorable="d"
        Title="{Binding ProjectName, StringFormat='Project Properties - {0}'}"
        Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource RegionBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/SkinDefault.xaml"/>
                <ResourceDictionary Source="pack://application:,,,/HandyControl;component/Themes/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Window.DataContext>
        <vm:ProjectPropertiesViewModel />
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,15">
            <TextBlock Text="Project Properties"
                       FontSize="24"
                       FontWeight="SemiBold"
                       Foreground="{DynamicResource PrimaryBrush}"/>
            <TextBlock Text="{Binding ProjectName}"
                       FontSize="14"
                       Margin="0,5,0,0"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Properties}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  SelectionMode="Extended"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternationCount="2"
                  RowHeaderWidth="0"
                  Margin="0,0,0,10"
                  BorderBrush="{DynamicResource BorderBrush}"
                  Background="White"
                  RowBackground="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID"
                                    Binding="{Binding DisplayId}"
                                    Width="80"/>
                <DataGridTextColumn Header="Property Name"
                                    Binding="{Binding PropertyName}"
                                    Width="*"/>
                <DataGridTextColumn Header="Value"
                                    Binding="{Binding PropertyValue}"
                                    Width="2*"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Footer -->
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <TextBlock Text="{Binding Properties.Count, StringFormat='Total: {0} properties'}"
                       VerticalAlignment="Center"
                       Margin="0,0,20,0"
                       Foreground="{DynamicResource SecondaryTextBrush}"/>
            <Button Content="Refresh"
                    Command="{Binding RefreshCommand}"
                    Style="{StaticResource ButtonPrimary}"
                    Width="100"/>
        </StackPanel>
    </Grid>
</Window>
